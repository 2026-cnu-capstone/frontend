# 백엔드 API 베이스 URL
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# MSW mock 토글 — true 일 때만 브라우저에서 service worker가 가로챈다.
# 백엔드 미가동 상태에서 프런트엔드만 띄워볼 때 사용.
NEXT_PUBLIC_ENABLE_MOCK=false
